apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loadbalancer
  annotations:
    kubernetes.io/ingress.className: "traefik"
spec:
  defaultBackend:
    service:
      name: argocd-server
      port:
        number: 443
